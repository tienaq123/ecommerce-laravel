<!-- resources/views/banners/index.blade.php -->
<!DOCTYPE html>
<html>

<head>
    <title>Banners</title>
</head>

<body>
    {{-- <h1>Banners</h1>

    @if(isset($error))
        <p style="color:red;">{{ $error }}</p>
    @endif

    <ul id="banners-list">
        @foreach($banners as $banner)
            <li>ID: {{ $banner['id'] }}, Title: {{ $banner['title'] }}</li>
        @endforeach
    </ul> --}}
</body>

</html>
